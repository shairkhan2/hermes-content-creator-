{
  "_comment": "The closed set the writer may draw from. A named person, place, object, or rule must exist here BEFORE prose can use it — inventing on the fly is how act three contradicts act one. The writer may propose additions: add to canon first, then write.",
  "premise": "<one sentence — what this story is>",
  "pov": "close third",
  "tense": "past",
  "entities": [
    {
      "id": "E001",
      "kind": "character",
      "name": "<name>",
      "facts": ["<one flat assertion each>", "<age, job, what they want>"]
    },
    {
      "id": "E002",
      "kind": "place",
      "name": "<name>",
      "facts": ["<what is true about it>"]
    },
    {
      "id": "E003",
      "kind": "object",
      "name": "<name>",
      "facts": ["<where it came from>", "<what is wrong with it>"]
    }
  ],
  "rules": [
    {
      "_comment": "The load-bearing part. A threat with no limits has no tension — the audience cannot tell what counts as a near miss. Good rules do not constrain the story, they produce it.",
      "id": "R001",
      "rule": "<what it can and cannot do, and when>",
      "consequence": "<what this forces the characters to do — this is where scenes come from>"
    }
  ]
}
