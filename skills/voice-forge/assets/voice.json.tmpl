{
  "_comment": "The per-project TTS backend choice. Asked once at the start of every project via clarify — never inferred from a global default, and never silently reused when a new project starts.",
  "backend": "elevenlabs",
  "voice": "<voice id or name>",
  "chosen_at": "<ISO date>"
}
